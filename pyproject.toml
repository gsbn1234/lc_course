#[project]
#name = "lc-course"
#version = "0.1.0"
#description = "Add your description here"
#readme = "README.md"
#requires-python = ">=3.14"
#dependencies = [
#    "langgraph-cli[inmem]>=0.4.31",
#]

[project]
name = "pythonproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain>=1.3.13",
    "langchain-deepseek>=1.1.0",
    "langchain-tavily>=0.2.18",
    "langgraph-cli[inmem]>=0.4.31",
    "redis>=8.0",   # Redis 会话存储（第 6 步：替代 backend 内存 dict）
]

[project.optional-dependencies]
# 开发/测试专用依赖：pip install -e ".[dev]" 或 uv sync --extra dev
dev = [
    "pytest>=8",
]
